# Cloudflare R2 Storage
AWS_ACCESS_KEY_ID=***************************
AWS_SECRET_ACCESS_KEY=***************************
AWS_S3_BUCKET=aloshield
AWS_S3_ENDPOINT=***************************
AWS_S3_REGION=auto

# Supabase
SUPABASE_URL=***************************
SUPABASE_ANON_KEY=***************************
SUPABASE_SERVICE_KEY=***************************

# Google OAuth
GOOGLE_CLIENT_ID=***************************
GOOGLE_CLIENT_SECRET=***************************
GOOGLE_CALLBACK_URL=https://chfywkuswfnjrzazbnxq.supabase.co/auth/v1/callback

# JWT
JWT_SECRET=***************************
JWT_REFRESH_SECRET=***************************
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Server
PORT=3001
WS_PORT=3002
NODE_ENV=development
FRONTEND_URL=https://aloshield.phucndh.site

# Redis (optional for local dev)
# For local dev: leave REDIS_HOST empty or unset to disable Redis
# For production with Docker: set REDIS_HOST=redis to use Docker Redis service
REDIS_HOST=
REDIS_PORT=6379
REDIS_PASSWORD=

# Email (SMTP) - For password reset emails
# Gmail example:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=ALO Shield <your-email@gmail.com>
#
# For Gmail, you need to create an App Password:
# 1. Go to Google Account settings
# 2. Security > 2-Step Verification > App passwords
# 3. Generate a new app password and use it as SMTP_PASSWORD
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

